# AI 工程辅助中心 — 系统完整说明

## 第一部分：系统概述

面向**设计团队**的智能问答 RAG 知识库系统，核心技术栈：

| 层级 | 技术 |
|------|------|
| 后端框架 | Spring Boot 2.7.18 |
| ORM | MyBatis-Plus 3.5.3 |
| LLM 编排 | LangChain4j 0.36.0 |
| LLM 模型 | DeepSeek (deepseek-chat)，通过 OpenAI 兼容协议调用 |
| 向量模型 | 阿里云 DashScope text-embedding-v3（稠密 1024 维 + 稀疏向量） |
| 向量数据库 | Pinecone（REST API 直连） |
| 文档解析 | Apache Tika 2.9.1 |
| 数据库 | MySQL 8.0 + Redis 6.0 |
| 前端 | Vue3 + Vite + Element Plus |
| 认证 | JWT（Access Token + Refresh Token） |

---

## 第二部分：系统架构

```
┌──────────────────────────────────────────────┐
│              前端 (Vue3 + Vite)                │
│  登录/注册 │ 知识库管理 │ 文件上传 │ 智能对话   │
│              长期记忆录入                      │
└──────────────────┬───────────────────────────┘
                   │ HTTP (JWT)
┌──────────────────▼───────────────────────────┐
│            Spring Boot 单体应用                │
│                                              │
│  auth/         认证模块（注册/登录/JWT）       │
│  knowledgebase/ 知识库CRUD + 文件上传服务       │
│  vector/        文本提取 + 语义分片 + 混合向量  │
│  rag/           RAG 查询（检索+重排+LLM回答）  │
│  memory/        短期记忆(Redis) + 长期记忆(MySQL)│
│  llm/           DeepSeek 调用封装              │
└──────────────────┬───────────────────────────┘
                   │
    ┌──────────────┼──────────────┐
    ▼              ▼              ▼
┌──────┐    ┌──────────┐   ┌──────────┐
│MySQL │    │  Redis   │   │ Pinecone │
│业务数据│   │短期记忆   │   │向量存储   │
└──────┘    └──────────┘   └──────────┘
```

---

## 第三部分：文件上传与处理流程

### 完整链路

```
POST /api/files/upload  (multipart/form-data)
    │
    ▼
FileController       → 接收 MultipartFile
    │
    ▼
FileService           → 校验格式/大小 → 存本地磁盘 → MySQL file 表
    │
    ▼
HybridVectorService   → 核心处理 ↓
    │
    ├─ ① 文本提取      TextExtractionService
    │     Apache Tika 自动识别格式(PDF/Word/MD/TXT) → 纯文本 String
    │
    ├─ ② 语义分片      TextChunkService
    │     正则切句子 → embedAll 逐句向量化 → 相邻余弦相似度
    │     → 相似度 ≥ 0.7 合并 / < 0.7 切断 → 控制 ≤500 字/块
    │     输出: List<TextSegment>
    │
    ├─ ③ BM25 增量训练  BM25Encoder
    │     首次: train(全部文档) / 后续: updateTrain(仅新分片)
    │     中英文混合分词: 中文 bigram+unigram / 英文单词
    │
    └─ ④ 逐块向量化 + 双写
          ├─ MySQL: INSERT chunk 表（备份原文）
          └─ Pinecone REST API: POST /vectors/upsert
               {
                 id: "chunk_123",
                 values: [0.12, -0.34, ...],        ← 稠密向量 1024维
                 sparse_values: {indices, values},   ← 稀疏 BM25 向量
                 metadata: {fileName, content, fileId}  ← 原文存此处
               }
```

### 各步骤对应代码

| 步骤 | 类 | 方法 |
|------|-----|------|
| HTTP 接收 | `FileController` | `uploadFile()` |
| 校验+调度 | `FileService` | `uploadAndProcess()` |
| 文本提取 | `TextExtractionService` | `extractText()` |
| 语义分片 | `TextChunkService` | `chunk()` |
| 稠密向量 | `HybridVectorService` | DashScope `text-embedding-v3` |
| 稀疏向量 | `BM25Encoder` | `embed()` |
| 向量存储 | `HybridVectorService` | Pinecone REST `/vectors/upsert` |
| 增量训练 | `BM25Encoder` | `updateTrain()` |

---

## 第四部分：语义分片详解

### 算法

```
输入文本
  │
  ├─ 1. 正则切句: (?<=[。！？.!?\n])(?=[^。！？.!?\n])
  │      保留标点在上一句末尾
  │
  ├─ 2. embedAll(sentenceSegments)
  │      每个句子调用 text-embedding-v3 → 1024维向量
  │
  ├─ 3. 计算相邻句子余弦相似度
  │      sim[i] = cosine(vec[i], vec[i+1])
  │
  └─ 4. 贪心合并
         遍历句子:
           sim[i] ≥ 0.7 AND 当前块字数 + 下句字数 ≤ 500
             → 合并
           否则
             → 保存当前块，下句作为新块开头
```

### 示例

```
文本:
  "色彩规范是公司设计体系的核心文档。"
  "主色调定义为#667eea蓝紫渐变。"
  "辅色搭配需要遵循对比度规则。"
  "另外，字体规范要求标题使用思源黑体。"
  "正文字体使用微软雅黑14px。"

余弦相似度:
  句0←→句1: 0.91  (色彩→主色调，同话题)
  句1←→句2: 0.87  (主色调→辅色，同话题)
  句2←→句3: 0.49  (色彩→字体，话题切换 ✂)
  句3←→句4: 0.93  (字体→字体，同话题)

结果:
  Chunk 1: 句0 + 句1 + 句2  (色彩相关，语义连贯)
  Chunk 2: 句3 + 句4        (字体相关，语义连贯)
```

---

## 第五部分：混合检索详解

### 写入时：双重向量化

```
每个 chunk 写入 Pinecone 时携带三种信息:

  values:        [0.12, -0.34, 0.67, ...]  ← 稠密向量(text-embedding-v3, 1024维)
  sparse_values: {indices: [5,23,67], values: [0.8, 1.2, 0.5]}  ← 稀疏向量(BM25)
  metadata:      {content: "原文...", fileName: "设计规范.pdf"}     ← 原文

三条数据存在 Pinecone 同一条记录里。
```

### 检索时：混合查询

```
用户问题 → Pinecone REST API POST /query
  {
    vector: [0.11, -0.33, ...],         ← 问题的稠密向量
    sparse_vector: {indices, values},    ← 问题的稀疏向量
    top_k: 10,                           ← 粗筛 10 条
    include_metadata: true               ← 返回原文
  }

Pinecone 内部同时用稠密+稀疏两种方式打分并融合
  → 返回 10 条 matches（含 score + metadata.content）
  → 取 topK × 2 = 10 条候选
```

### 重排序（Rerank）

```
10 条候选 → RerankService.rerank(query, chunks, topK=5)

当前策略: keyword（关键词匹配重排）
  finalScore = Pinecone分数 × 0.7 + 关键词命中boost × 0.3
  关键词越靠前、命中比例越高 → 加分越多

可选策略:
  score:  纯 Pinecone 分数排序
  keyword: 关键词位置+密度加权（默认）
  llm:    LLM 直接判断相关性排序
```

---

## 第六部分：RAG 问答完整流程

```
用户输入: "那色彩部分呢？"   (结合上下文，上一轮在讨论设计规范)
    │
    ├─ 1. Query Rewriting（查询重写）
    │      从短期记忆取最近对话 → LLM 重写为独立问题
    │      → "设计规范中关于色彩部分有哪些要求？"
    │      用途: 仅用于检索，不进入最终 Prompt
    │
    ├─ 2. RAG 检索 + Rerank
    │      rewrittenQuery → Pinecone 混合检索 → 10 条候选
    │      → keyword 重排序 → 取 top 5
    │
    ├─ 3. 长期记忆召回
    │      rewrittenQuery → 向量化 → MySQL 余弦相似度 → top 3
    │
    └─ 4. 构建 Prompt → LLM 生成
           ┌─ SystemMessage ────────────────────────┐
           │  系统提示 + RAG top5 chunks             │
           │  + 长期记忆 top3                        │
           ├─ UserMessage ──── 第1轮用户消息          │
           ├─ AiMessage ───── 第1轮AI回复   ← 短期   │
           ├─ UserMessage ──── 第2轮用户消息   记忆    │
           ├─ AiMessage ───── 第2轮AI回复            │
           ├─ ...                                   │
           └─ UserMessage ──── "那色彩部分呢？"  ← 原始问题

关键设计:
  - rewrittenQuery → 用于检索（让召回更精准）
  - originalQuery + 对话历史 → 用于 LLM Prompt（让回答更自然连贯）
  - conversation-rounds: 5（每轮 = user+assistant 各一条）
```

---

## 第七部分：记忆系统详解

### 短期记忆（ShortTermMemoryService）

```
存储: Redis List
Key:  stm:{userId}:{sessionId}
Value: JSON 数组 [{role:"user",content:"..."}, {role:"assistant",content:"..."}]

滑动窗口机制:
  ① 每次 addMessage() 后自动检查总字符数
  ② 超过 32,000 字符 → 触发摘要
  ③ 摘要策略: (超出32k的部分 + 窗口内前一半) → LLM 摘要
  ④ 保留: [摘要] + [窗口内后一半内容]
  ⑤ TTL: 24 小时

Prommpt 融合:
  最近 5 轮对话（10 条消息）直接作为 LangChain4j ChatMessage 列表
  放入 LLM generate() 请求中
```

### 长期记忆（LongTermMemoryService）

```
存储: MySQL user_memory 表
字段: {userId, memoryType, content, embedding(JSON数组), importanceScore}

写入:
  ① RAG 每次回答后自动触发 extractAndStoreMemories()
  ② LLM 从对话中提取重要偏好/信息
  ③ embedding 向量化 → JSON 存入 MySQL

召回:
  ① 用户查询 → 向量化
  ② 从 MySQL 拉取该用户所有记忆
  ③ 逐条计算余弦相似度
  ④ 过滤 ≥0.6 的 → 排序 → 取 top 3

手动录入:
  MemoryController: POST /api/memory/long-term
  前端: 侧边栏「长期记忆」Tab → + 录入记忆
```

---

## 第八部分：项目结构

```
src/main/java/com/knowledgemanager/
│
├── KnowledgeManagerApplication.java   启动类
│
├── auth/                              认证模块
│   ├── controller/AuthController      POST /api/auth/login|register|refresh
│   └── service/AuthService            注册/登录/Token刷新
│
├── knowledgebase/                     知识库模块
│   ├── controller/KnowledgeBaseController  CRUD /api/knowledge-bases
│   ├── controller/FileController       POST /api/files/upload
│   └── service/FileService            文件校验+调度处理
│
├── vector/                            向量化模块
│   ├── config/VectorConfig            EmbeddingModel Bean
│   ├── service/TextExtractionService  Apache Tika 文本提取
│   ├── service/TextChunkService       语义相似度分片
│   ├── service/HybridVectorService    Pinecone 混合向量写入+检索+删除
│   ├── service/RerankService          重排序（keyword/llm/score）
│   └── service/BM25Encoder.java       BM25 编码器（含增量训练+中文分词）
│
├── rag/                               RAG 问答模块
│   ├── controller/RAGController       POST /api/rag/query
│   └── service/RAGService             完整 RAG 流程编排
│
├── memory/                            记忆系统模块
│   ├── controller/MemoryController    GET|POST|DELETE /api/memory/long-term
│   ├── config/RedisConfig             Redis 配置
│   ├── service/ShortTermMemoryService Redis 短期记忆（32k滑动窗口+摘要）
│   └── service/LongTermMemoryService  MySQL 长期记忆（向量化+余弦召回）
│
├── llm/                               LLM 模块
│   ├── config/LLMConfig               DeepSeek (OpenAiChatModel)
│   └── service/LLMService             对话/摘要/信息提取
│
└── common/                            公共模块
    ├── entity/                        实体类(User/KnowledgeBase/File/Chunk/...)
    ├── dto/                           DTO
    ├── mapper/                        MyBatis-Plus Mapper
    ├── config/                        SecurityConfig/WebMvcConfig/MybatisPlusConfig
    ├── interceptor/JwtInterceptor     JWT 认证拦截器
    ├── util/                          JwtUtil/PasswordUtil/IdGenerator
    └── exception/                     全局异常处理
```

---

## 第九部分：API 端点

### 认证
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/auth/register` | 注册 |
| POST | `/api/auth/login` | 登录 |
| POST | `/api/auth/refresh` | 刷新 Token |

### 知识库
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/knowledge-bases` | 创建知识库 |
| GET | `/api/knowledge-bases` | 知识库列表 |
| GET | `/api/knowledge-bases/{id}` | 知识库详情 |
| PUT | `/api/knowledge-bases/{id}` | 更新知识库 |
| DELETE | `/api/knowledge-bases/{id}` | 删除知识库 |

### 文件
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/files/upload` | 上传并自动处理 |
| GET | `/api/files?knowledgeBaseId=` | 文件列表 |
| DELETE | `/api/files/{id}` | 删除文件+向量 |

### RAG 问答
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/rag/query` | 智能问答 (Header: X-Session-Id) |

### 长期记忆
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/memory/long-term` | 记忆列表 |
| POST | `/api/memory/long-term` | 手动录入 |
| DELETE | `/api/memory/long-term/{id}` | 删除记忆 |

---

## 第十部分：配置与环境变量

```bash
# 必配
DASHSCOPE_API_KEY    # 阿里云 DashScope（向量模型 text-embedding-v3）
DEEPSEEK_API_KEY      # DeepSeek（LLM 对话）
MYSQL_HOST / MYSQL_PASSWORD
REDIS_HOST / REDIS_PASSWORD

# 可选（生产环境）
PINECONE_API_KEY      # Pinecone 向量数据库
PINECONE_INDEX_URL    # Pinecone index URL
TENCENT_COS_*         # 腾讯云对象存储
```

---

## 第十一部分：快速启动

```bash
# 1. 初始化数据库
mysql -u root -p < init-scripts/mysql/01-init-database.sql

# 2. 配置环境变量
export DASHSCOPE_API_KEY=sk-xxx
export DEEPSEEK_API_KEY=sk-xxx
export MYSQL_PASSWORD=xxx

# 3. 启动后端
mvn spring-boot:run

# 4. 启动前端（开发模式）
cd frontend && npm install && npm run dev

# 5. 访问 http://localhost:3000
```
